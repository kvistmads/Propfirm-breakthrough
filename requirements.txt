# Python 3.14.6 — det .venv faktisk kører (låst 2026-09-13, fase 2).
# Der er INGEN scipy. Ruinmodellen og statistikken klarer sig med numpy og math.
#
# Denne fil indeholder KUN det der er installeret i .venv, så
#     pip install -r requirements.txt
# virker fra en frisk klon. Det gjorde den ikke før 2026-09-17: afsnit 2 lå i samme
# fil med en kommentar om at det ikke skulle installeres, men pip læser ikke
# kommentarer. chromadb trækker onnxruntime, som ikke har hjul til 3.14, og de gamle
# chromadb-versioner kræver numpy<2.0 mod vores 2.5.3. Resultatet var
# ResolutionImpossible efter ti minutters backtracking gennem 60 chromadb-versioner.
#
# Arvede pakker fra det gamle repo ligger nu i requirements-arvet.txt.
# De må IKKE installeres sammen med denne fil.

pandas==3.0.5
numpy==2.5.3
pyarrow==25.0.1
PyYAML==6.0.3
python-dotenv==1.2.3
pytest==9.1.1
pytest-asyncio==1.4.0
aiohttp==3.14.3

# Fase 1 (propfirm) — datalaget i data/. NYSE-kalenderen til RTH-markering og
# Databento-klienten til CME Globex (GLBX.MDP3). Yahoo NQ=F kun som krydstjek (K2).
exchange_calendars==4.13.2
databento==0.86.0
yfinance==1.7.0
